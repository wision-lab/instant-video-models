freeze_initial_params: true
lambda: 0.4
loss:
  _target: instant_video_models.metrics.unified_loss.UnifiedLoss
  loss_metric:
    _target: instant_video_models.losses.rmse.RMSE
    epsilon: 1e-6
  stability_weight: ${..lambda}
  mask_nan: false
