_defaults:
  - ./_defaults_evaluate.yml
  - configs/stabilizers/depth_anything/controlled.yml
_output_dir: outputs/${_config_dir}/${_name}/${lambda}/${_datetime}
override_weights_filepath: ${_original_dir}/WEIGHTS_FILEPATH
