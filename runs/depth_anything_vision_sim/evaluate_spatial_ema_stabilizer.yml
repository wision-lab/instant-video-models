_defaults:
  - configs/evaluate/evaluate_depth_anything_vision_sim.yml
  - configs/stabilizers/depth_anything/spatial_ema_stabilizer.yml

# Use lambda to create a subdir instead of appending to the name.
_name: evaluate_spatial_ema_stabilizer
_output_dir: outputs/${_config_dir}/${_name}/${lambda}/${_datetime}
