_defaults:
  - ./train_spatial_ema_stabilizer_rain_unfrozen.yml
_name: train_spatial_ema_stabilizer_rain_unfrozen_ablation

# Confirm that adding stabilizers helps when the base model is unfrozen.
controllers: [ ]
stabilizers: [ ]
