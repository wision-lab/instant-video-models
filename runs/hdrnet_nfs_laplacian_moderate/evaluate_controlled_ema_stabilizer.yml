_defaults:
  - ./_defaults_evaluate.yml
  - configs/stabilizers/hdrnet/controlled_ema_stabilizer.yml

# Use lambda to create a subdir instead of appending to the name.
_name: evaluate_controlled_ema_stabilizer
_output_dir: outputs/${_config_dir}/${_name}/${lambda}/${_datetime}
